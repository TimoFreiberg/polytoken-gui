{
  "name": "pilot",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": ["protocol", "server", "client"],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "test": "bun test protocol server",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
