{
  "name": "pilot",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": ["protocol", "client", "desktop"],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:client": "bun run --cwd client dev",
    "build": "bun run --cwd client build",
    "test": "bun test protocol client/src scripts",
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots",
    "test:e2e:live": "playwright test -c playwright.live.config.ts",
    "check": "tsc -p protocol/tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit && tsc -p tsconfig.e2e.json --noEmit && tsc -p tsconfig.parity.json --noEmit && bun run --cwd client check",
    "check:rs": "cd server-rs && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test",
    "deploy:now": "bash scripts/deploy-now.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/bun": "^1.3.14",
    "openapi-typescript": "^7.13.0",
    "typescript": "^6.0.3"
  }
}
